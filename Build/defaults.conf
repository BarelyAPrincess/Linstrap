declare -u INITCONF_PREFIX="LINSTRAP_BUILDCONF"
declare -A LINSTRAP_BUILDCONF_ARCH=([TITLE]="cpu arch" [QUESTION]="What CPU arch would you like to use?" [DEFAULT]="x86_64" [OPTIONS]="x86_64 x86" )
declare -A LINSTRAP_BUILDCONF_BOOT_METHOD=([TITLE]="boot method" [QUESTION]="Which boot method would you like?" [DEFAULT]="EFI" [OPTIONS]="BIOS EFI" )
declare -A LINSTRAP_BUILDCONF_DIRTY_SETUP=([DEFAULT]="1" )
declare -A LINSTRAP_BUILDCONF_INITRD_BRANCH=([TITLE]="initrd branch" [QUESTION]="What initrd branch would you like to setup?" [DEFAULT]="bleeding" [OPTIONS]="bleeding stable custom" )
